[package]
name = "gee"
description = "An experimental Python WSGI."
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["full"] }
pyo3 = "0.2"
tokio = { version = "1", features = ["full"] }
